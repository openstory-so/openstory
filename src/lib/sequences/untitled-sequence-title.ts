/**
 * Placeholder title a sequence carries from the click until scene-split names
 * it from the script. Prompts that run before that rename (the poster) must
 * treat it as "no title", not as a title (#1453).
 */
export const UNTITLED_SEQUENCE_TITLE = 'Untitled Sequence';
